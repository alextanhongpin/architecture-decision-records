A good way to name dynamic methods is:

```
verb, noun with config
```
For example, `SearchUser(ctx, query, limit)`

If no config is required, we can just use 

```
verb, noun
```

For example:

```
OpenPage(ctx, url)
```
